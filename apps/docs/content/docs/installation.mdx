---
title: Installation
icon: SquareChevronRight
---

import { Accordion, Accordions } from "fumadocs-ui/components/accordion";

## Setup Guide

<div className="steps">

<div className="step">
Clone [source repository](https://github.com/cuhacking/hackathon).

    <Callout type="warn">
    If you'd like to contribute, please fork first and then clone your fork.

    See the [Contribution Guidelines - Getting Started](/contribution-guidelines/getting-started) to learn more.
    </Callout>

<Tabs groupId="git-clone-method" items={['GitHub CLI (recommended)','HTTPS', 'SSH']} persist>
<Tab value="GitHub CLI (recommended)">

GitHub CLI handles much of the authentication and other configuration for you. It can also be used to view/make issues and pull requests, and also access the project board from the terminal.

Ensure [GitHub CLI](https://cli.github.com/) is installed.

```sh title="Terminal"
 gh repo clone cuhacking/2025
```

</Tab>
  <Tab value="HTTPS">

```sh title="Terminal"
 git clone https://github.com/cuhacking/2025.git
```

</Tab>
<Tab value="SSH">

```sh title="Terminal"
 git clone git@github.com:cuhacking/2025.git
```

</Tab>
</Tabs>

  </div>
  <div className="step">
    Navigate to directory and install dependencies.

```sh title="terminal"
cd 2025
pnpm i
```

  </div>

  <div className="step">
    Install [Playwright](https://playwright.dev/) browsers.
    ```sh title="terminal"
    pnpm exec playwright install
    ```
  </div>

  <div className="step">
  Run the project of your choice.

`--verbose` provides detailed error logs.

<Tabs groupId="projects" items={['Website', 'Portal', 'Docs', 'Storybook', 'ESLint Config Inspector']} persist>
  <Tab value="Website">
    <Tabs groupId="website-project-nx-commands" items={['dev', 'build', 'start']} persist>
      <Tab value="dev">
        ```sh title="Terminal"
        pnpm nx dev website --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run website:dev --verbose
        ```
      </Tab>
      <Tab value="build">
        ```sh title="Terminal"
        pnpm nx build website --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run website:build --verbose
        ```
      </Tab>
      <Tab value="start">
        ```sh title="Terminal"
        pnpm nx start website --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run website:start --verbose
        ```
      </Tab>
    </Tabs>
  </Tab>

  <Tab value="Portal">
    <Tabs
      groupId="portal-project-nx-commands"
      items={["dev", "build", "start"]}
      persist
    >
      <Tab value="dev">
        ```sh title="Terminal"
        pnpm nx dev portal --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run portal:dev --verbose
        ```
      </Tab>

      <Tab value="build">
        ```sh title="Terminal"
        pnpm nx build portal --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run portal:build --verbose
        ```
      </Tab>

      <Tab value="start">
        ```sh title="Terminal"
        pnpm nx start portal --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run portal:start --verbose
        ```
      </Tab>

    </Tabs>
  </Tab>

  <Tab value="Docs">
    <Tabs groupId="docs-project-nx-commands" items={['dev', 'build', 'start']} persist>
      <Tab value="dev">
        ```sh title="Terminal"
        pnpm nx dev docs --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run docs:dev --verbose
        ```
      </Tab>
      <Tab value="build">
        ```sh title="Terminal"
        pnpm nx build docs --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run docs:build --verbose
        ```
      </Tab>
      <Tab value="start">
        ```sh title="Terminal"
        pnpm nx start docs --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run docs:start --verbose
        ```
      </Tab>
    </Tabs>
  </Tab>

  <Tab value="Storybook">
    <Tabs
      groupId="storybook-project-nx-commands"
      items={["dev", "build", "start"]}
      persist
    >
      <Tab value="dev">
        ```sh title="Terminal"
        pnpm nx dev storybook --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run storybook:dev --verbose
        ```
      </Tab>

      <Tab value="build">
        ```sh title="Terminal"
        pnpm nx build storybook --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run storybook:build --verbose
        ```
      </Tab>

      <Tab value="start">
        ```sh title="Terminal"
        pnpm nx start storybook --verbose
        ```
        Or
        ```sh title="Terminal"
        pnpm nx run storybook:start --verbose
        ```
      </Tab>

    </Tabs>
  </Tab>
  <Tab value="ESLint Config Inspector">
        ```sh title="Terminal"
        pnpm lint:inspect
        ```
      </Tab>

</Tabs>

Explore the project graph to see the projects in the monorepo and the tasks you can run with Nx.

```sh title="Terminal"
pnpm nx graph --verbose
```

<Accordions>
  <Accordion title="What's the general syntax of Nx commands?">
  <img src="https://nx.dev/documentation/shared/images/run-target-syntax.svg" />

You can also run multiple targets:

```sh title="Terminal"
pnpm nx run-many -t <target1> <target2>
```

..or add `-p` to filter specific projects

```sh title="Terminal"
pnpm nx run-many -t <target1> <target2> -p <proj1> <proj2>
```

Targets can be defined in the `package.json` or `projects.json`. [Learn more](https://nx.dev/features/run-tasks).

[Learn more](https://nx.dev/core-features/explore-graph).

Use [Nx Console](https://nx.dev/nx-console) to integrate with editors.

<img src="https://nx.dev/documentation/shared/images/nx-console/nx-console-screenshot.webp" />

</Accordion>
</Accordions>

  </div>
  <div className="step">
You'll see the url(s) in your terminal for the project(s) you're running, simply paste those in to your browser address bar.

    For example you may see:

```sh title="Terminal"
╰─ pnpm nx dev docs --verbose                                                                                                                   ─╯

> nx run docs:dev

> next dev

[MDX] initialized map file
   ▲ Next.js 15.0.3
   - Local:        http://localhost:3000
   - Experiments (use with caution):
     · turbo

 ✓ Starting...
   automatically enabled Fast Refresh for 1 custom loader
 ✓ Ready in 1790ms
```

<Callout type="warn" title="URLs may change as the project matures.">

If multiple apps are running or another server using that network port, your URLs may be different from someone else's and that's okay.

Always look at your terminal for the correct URL.
</Callout>

</div>
</div>

## What's next?

Explore the [Tech Stack](/tech-stack) to learn what powers our meta-framework.

### Get Involved

Looking to contribute? If you've already read the [Overview](/) then head on over to the [Contribution Guidelines](/contribution-guidelines).

### Connect With Us

Connect on our channels and with the cuHacking team to ask questions, get help and keep up to date with the latest events.

- ⭐ [Star us on GitHub](https://github.com/cuhacking/2025) to show your support.
- Join our [Discord Community](https://discord.com/invite/h2cQqF9aZf).
- Follow us on [Social Media](https://linktr.ee/cuhacking_).
