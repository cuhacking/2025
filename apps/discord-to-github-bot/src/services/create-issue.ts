import type { GitHubIssue } from '@src/types/github-issue'

export function gaurdAgainstInvalidIssues(issueContent: object) {
  return issueContent
}

export function createIssue(issueContent: object) {
  return issueContent
}
